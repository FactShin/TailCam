name: Publish to PyPI

# Trusted Publishing — no PyPI token in GitHub secrets.
#
# tailcam is already published. Maintain this Trusted Publisher in the
# existing PyPI project's publishing settings (no new pending publisher):
#   PyPI project name:  tailcam
#   Owner:              FactShin
#   Repository:         TailCam
#   Workflow name:      pypi-publish.yml
#   Environment:        pypi
#
# Bump the package version first, then create a GitHub Release (tag vX.Y.Z)
# at the merged commit OR run this workflow manually from main. Do not run
# both for the same version; PyPI distribution filenames cannot be reused.

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pypi:
    name: Build and publish
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/tailcam/
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build sdist and wheel
        run: |
          pip install --upgrade build
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
